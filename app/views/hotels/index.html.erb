<h1>Listing hotels</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Country</th>
    <th>City</th>
    <th>Number of rooms single</th>
    <th>Number of rooms doubles</th>
    <th>Number of rooms married</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @hotels.each do |hotel| %>
  <tr>
    <td><%= hotel.name %></td>
    <td><%= hotel.country %></td>
    <td><%= hotel.city %></td>
    <td><%= hotel.number_of_rooms_single %></td>
    <td><%= hotel.number_of_rooms_doubles %></td>
    <td><%= hotel.number_of_rooms_married %></td>
    <td><%= link_to 'Show', hotel %></td>
    <td><%= link_to 'Edit', edit_hotel_path(hotel) %></td>
    <td><%= link_to 'Destroy', hotel, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Hotel', new_hotel_path %>
