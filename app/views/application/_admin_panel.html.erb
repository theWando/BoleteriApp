      <% if user_signed_in? %>
        <div class="span3">
        <% if current_user.user_type == "admin" %>
          <div class=" bs-docs-sidenav well" >
            <ul class="nav nav-list affix-top">
              <li class="nav-header">Administración</li>
              <li><%= link_to "Crear Viaje", new_travel_path %></li>
              <li><%= link_to "Crear Hotel", new_hotel_path %></li>
            </ul>
          </div>
        <% end %>
        </div>
      <% end %>

